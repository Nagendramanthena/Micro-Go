{{template "base" .}}

{{define "content" }}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="mt-5">Test microservices</h1>
                <hr>

                <a id="brokerbtn" class="btn btn-outline-secondary" href="javascript:void(0);">Test Broker</a>
                <a id="Authservicebtn" class="btn btn-outline-secondary" href="javascript:void(0);">Test Auth</a>
                <a id="loggerservicebtn" class="btn btn-outline-secondary" href="javascript:void(0);">Test Logger</a>
                <a id="mailservicebtn" class="btn btn-outline-secondary" href="javascript:void(0);">Test Mail</a>
                <a id="blogserviceAddbtn" class="btn btn-outline-secondary" href="javascript:void(0);">Push Blog</a>
                <a id="blogservicegettn" class="btn btn-outline-secondary" href="javascript:void(0);">Get Blog</a>


                <div id="output" class="mt-5" style="outline: 1px solid silver; padding: 2em;">
                    <span class="text-muted">Output shows here...</span>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col">
                <h4 class="mt-5">Sent</h4>
                <div class="mt-1" style="outline: 1px solid silver; padding: 2em;">
                    <pre id="payload"><span class="text-muted">Nothing sent yet...</span></pre>
                </div>
            </div>
            <div class="col">
                <h4 class="mt-5">Received</h4>
                <div class="mt-1" style="outline: 1px solid silver; padding: 2em;">
                    <pre id="received"><span class="text-muted">Nothing received yet...</span></pre>
                </div>
            </div>
        </div>
    </div>
{{end}}

{{define "js"}}
    <script>
     
     let borkerbtn = document.getElementById("brokerbtn");
     let authservicebtn = document.getElementById("Authservicebtn");
     let loggerservicebtn = document.getElementById("loggerservicebtn");
     let output = document.getElementById("output");
     let sent = document.getElementById("payload");
     let received = document.getElementById("received");
     let mailbtn = document.getElementById("mailservicebtn");
     let Addblog = document.getElementById("blogserviceAddbtn");
     let getBlog = document.getElementById("blogservicegettn");

     mailbtn.addEventListener("click",function(){
        
        const payload = {
            action : "mail",
            mail : {
                From : "me@example.com",
                To : "you@there.com",
                Subject : "Hello there",
                Message : "Hi There you have succesfully built a mail microservice",
            }
        }
        
        const headers = new Headers();
        headers.append("Content-type","application/json");

        const body = {
            method : 'POST',
            body : JSON.stringify(payload),
            headers : headers,
        }

        fetch("http:\/\/localhost:8080/handle",body)
        .then((response) => response.json())
        .then((data) => {
            sent.innerHTML = JSON.stringify(payload,undefined,4);
            received.innerHTML = JSON.stringify(data,undefined,4);

            if(data.error){
                output.innerHTML += `<br><strong>Error:</strong> ${data.message}`;
            }else{
                output.innerHTML = `<br><strong>Response from broker service</strong>: ${data.message}`;
            }
        })
        .catch((error) => {
            output.innerHTML  += "<br><br>Error:" + error;
        })
     })


     loggerservicebtn.addEventListener("click",function(){

        const payload = {
            action : "log",
            log : {
                name : "event",
                message : "pushing some data as a result of calling logger service through broker service",
            }
        }

        const headers = new Headers();
        headers.append("Content-Type","application/json")
        const body = {
            method : "POST",
            body : JSON.stringify(payload),
            headers : headers
        }

        fetch("http:\/\/localhost:8080/handle",body)
        .then((response) => response.json())
        .then((data) => {
            sent.innerHTML = JSON.stringify(payload,undefined,4);
            received.innerHTML = JSON.stringify(data,undefined,4);

            if(data.error){
                output.innerHTML += `<br><strong>Error:</strong> ${data.message}`;
            }else{
                output.innerHTML = `<br><strong>Response from broker service</strong>: ${data.message}`;
            }
        })
        .catch((error) => {
            output.innerHTML  += "<br><br>Error:" + error;
        })
        
     })


     authservicebtn.addEventListener("click",function(){
        
        const payload = {
            action : "auth",
            auth : {
                email : "admin@example.com",
                password : "verysecret",
            }
        }
        
        const headers = new Headers();
        headers.append("Content-type","application/json");

        const body = {
            method : 'POST',
            body : JSON.stringify(payload),
            headers : headers,
        }

        fetch("http:\/\/localhost:8080/handle",body)
        .then((response) => response.json())
        .then((data) => {
            sent.innerHTML = JSON.stringify(payload,undefined,4);
            received.innerHTML = JSON.stringify(data,undefined,4);

            if(data.error){
                output.innerHTML += `<br><strong>Error:</strong> ${data.message}`;
            }else{
                output.innerHTML = `<br><strong>Response from broker service</strong>: ${data.message}`;
            }
        })
        .catch((error) => {
            output.innerHTML  += "<br><br>Error:" + error;
        })
     })



     borkerbtn.addEventListener("click",function(){
        const body = {
            method: "POST",
        }

        fetch("http:\/\/localhost:8080",body)
        .then((response) => response.json())
        .then((data) => {
            sent.innerHTML = "empty post request";
            received.innerHTML = JSON.stringify(data,undefined,4);

            if(data.error){
                console.log(data.message);
            }else{
                output.innerHTML = `<br><strong>Response from broker service</strong>: ${data.message}`;
            }
        })
        .catch((error) => {
            output.innerHTML  += "<br><br>Error:" + error;
        })
     })

     blogserviceAddbtn.addEventListener("click",function(){

        const payload = {
            action : "blog",
            log : {
                id:1,
                name : "Redis demo",
                data : "This is about redis which is an in memory database fastest cache in the world",
            }
        }

        const headers = new Headers();
        headers.append("Content-Type","application/json")
        const body = {
            method : "POST",
            body : JSON.stringify(payload),
            headers : headers
        }

        fetch("http:\/\/localhost:8080/handle",body)
        .then((response) => response.json())
        .then((data) => {
            sent.innerHTML = JSON.stringify(payload,undefined,4);
            received.innerHTML = JSON.stringify(data,undefined,4);

            if(data.error){
                output.innerHTML += `<br><strong>Error:</strong> ${data.message}`;
            }else{
                output.innerHTML = `<br><strong>Response from broker service</strong>: ${data.message}`;
            }
        })
        .catch((error) => {
            output.innerHTML  += "<br><br>Error:" + error;
        })
        
     })

     blogservicegettn.addEventListener("click",function(){

        fetch("http://localhost:8080/handleread", {
    method: "GET",
    headers: {
        'Content-Type': 'application/json',
        // Add any other headers your server expects
    }
})
.then(response => {
    console.log('Response status:', response.status);
    return response.json(); // Use .json() instead of .text() for JSON responses
})
.then(data => {
    console.log('Response data:', data);
    sent.innerHTML = JSON.stringify("Request successful", undefined, 4);
    received.innerHTML = JSON.stringify(data, undefined, 4);
    
    if (data.error) {
        output.innerHTML += `<br><strong>Error:</strong> ${data.message}`;
    } else {
        output.innerHTML += `<br><strong>Success:</strong> ${data.message || 'Data received'}`;
    }
})
.catch(error => {
    console.error('Error:', error);
    output.innerHTML += `<br><strong>Error:</strong> ${error.message}`;
});

                
            })

        
    </script>
{{end}}
